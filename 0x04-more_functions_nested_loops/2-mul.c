#include "main.h"
/**
 * main - Prints the result of two numbers multiplied together
 * @x - First number to be operated on
 * @y - Second number to be operated on
 * Return: The result of the xy multiplication
*/

int mul(int x, int y)
{
	return (x * y);
}
